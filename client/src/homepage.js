import { mainHeadingAnimation } from "./js/gsap.js";

document.addEventListener("DOMContentLoaded", () => {
  //This animation is removed during development to prevent the animation from playing every time the page is refreshed.
  mainHeadingAnimation();
});